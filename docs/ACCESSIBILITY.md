# Accessibility

Controls must be keyboard usable, labeled, visibly focused, and readable. Important numerical values should be exposed as text, not only through visuals.

Canvas/WebGL simulations should provide meaningful descriptions or telemetry where practical.

Support `prefers-reduced-motion`. Reduced motion must not break the educational state of a simulation.
